{% extends 'base.html.twig' %}

{% block title %}Diploma{% endblock %}

{% block body %}
    <h1>Diploma</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ diploma.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ diploma.name }}</td>
            </tr>
            <tr>
                <th>Author</th>
                <td>{{ diploma.author }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ diploma.date ? diploma.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>File</th>
                <td>{{ diploma.file }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ diploma.createdAt ? diploma.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ diploma.updatedAt ? diploma.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('ro_diploma_index') }}">back to list</a>

    <a href="{{ path('ro_diploma_edit', {'id': diploma.id}) }}">edit</a>

    {{ include('diploma/_delete_form.html.twig') }}
{% endblock %}
