{% extends 'base.html.twig' %}

{% block title %}ScientificInterest{% endblock %}

{% block body %}
    <h1>ScientificInterest</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ scientific_interest.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ scientific_interest.name }}</td>
            </tr>
            <tr>
                <th>NameCanonical</th>
                <td>{{ scientific_interest.nameCanonical }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('scientific_interest_index') }}">back to list</a>

    <a href="{{ path('scientific_interest_edit', {'id': scientific_interest.id}) }}">edit</a>

    {{ include('scientific_interest/_delete_form.html.twig') }}
{% endblock %}
