<form method="post" action="{{ path('scientific_interest_delete', {'id': scientific_interest.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ scientific_interest.id) }}">
    <button class="btn">Delete</button>
</form>
